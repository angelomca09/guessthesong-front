function getCollections() {
    let collections = [
        {
            name: "Games",
            playlistId: "12645052643"
        }
    ]

    // Add the collections from localStorage

    return collections;
}